# TODO print this in the bootstrap script
# mkdir $HOME/.nixpkgs
# erb config.nix.erb > $HOME/.nixpkgs/config.nix
#
pkgs:
 {
   packageOverrides = self: {
     nix = self.nix.override {
       storeDir = "<%= ENV['HOME'] %>/nix/store";
       stateDir = "<%= ENV['HOME'] %>/nix/var";
     };
   };
 }
